<!-- res/drawable/cardview_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Warna background card -->
    <corners android:radius="10dp"/> <!-- Radius sudut kartu -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray"/> <!-- Warna border -->
</shape>
